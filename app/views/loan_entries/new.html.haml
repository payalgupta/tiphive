%h1= @movie.name

.row
  .col-md-4
    = simple_form_for [@movie, LoanEntry.new] do |f|
      = f.input :user_id, collection: User.all, label_method: :email, value_method: :id, prompt: '-- Select a User --'
      = f.input :preferred_return_date
      = f.submit 'Save', class: 'btn btn-success'